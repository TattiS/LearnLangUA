export const selectMetaLanguages = (state) => state.filtersMeta.languages;

export const selectMetaLevels = (state) => state.filtersMeta.levels;

export const selectMetaPrices = (state) => state.filtersMeta.prices;

export const selectFiltersMetaLoading = (state) => state.filtersMeta.loading;

export const selectFiltersMetaError = (state) => state.filtersMeta.error;
